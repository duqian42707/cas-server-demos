# Application properties that need to be
# embedded within the web application can be included here

server:
  port: 8443
cas:
  webflow:
    crypto:
      # 为了避免异常，暂时关闭 java.security.InvalidAlgorithmParameterException: AlgorithmParameterSpec not of GCMParameterSpec
      enabled: false
